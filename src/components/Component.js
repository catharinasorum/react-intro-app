import React from 'react'

const Component = (props) => {
    return(
        <div>
            <p>Dette er den første subkomponenten. Den er veldig enkel, og den returnerer bare denne teksten.</p>
        </div>
    )
}

export default Component